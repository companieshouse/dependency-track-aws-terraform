aws_profile = "shared-services-eu-west-2"
aws_region = "eu-west-2"
desired_task_count = 1
environment = "common"
service_autoscale_enabled = false
service_autoscale_target_value_cpu = 1
service_scaledown_schedule = "30 20 ? * FRI *"
service_scaleup_schedule = "30 0 ? * MON *"
use_fargate = true
max_task_count = 1
sidecar_port = 8081
